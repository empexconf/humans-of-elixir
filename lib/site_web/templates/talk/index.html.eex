<%= render SharedView, "nav.html", conn: @conn %>

<div class="body style-2018 talks">
  <div class="normal-section">
    <h1>EMPEX Talks</h1>

    <p>
      Here they are, all in one place - every EMPEX talk. Be entertained, and be enlightened.
    </p>
  </div>

  <div class="small-section">
    <%= for {event, talks} <- Talks.group_by_event(@talks) do %>
      <h2>
        <%= event.name %>
      </h2>

      <%= for talk <- talks do %>
        <%= render "_talk_summary.html", conn: @conn, talk: talk %>
      <% end %>
    <% end %>
  </div>
</div>